type IService = {
    id: string;
    name: string;
    price: string;
    description: string;
    image: string;
    category: string;
    date: string;
    status: string;
    createdAt: string;
    updatedAt: string;
};

export type { IService };
